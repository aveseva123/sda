"""DrawingSet add-in library.

Pure modules (no ``adsk`` import, unit-testable under plain CPython):
    config, naming, bom, explode, textutil

Fusion-only modules (import ``adsk`` lazily):
    capabilities, model, explode_fusion, drawing_driver, export, pipeline, ui, log
"""
