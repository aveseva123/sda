# PLAN.md — план работ

Этапы и признаки готовности — раздел 19 `SPEC.md`. Здесь — разбивка текущего этапа на задачи.
Оценки — в часах чистой работы разработчика.

## Этап M0 — каркас (выполнен)

Признак готовности: `pytest` зелёный, окно открывается.

| # | Задача | Оценка | Статус |
|---|--------|-------:|--------|
| 0.1 | Репозиторий и окружение: `pyproject.toml`, Python 3.12, `uv`, зависимости стека (раздел 4), `ruff`, `mypy --strict` на `cam/core`, `pytest`/`pytest-qt`, CI на GitHub Actions | 2 | ✅ |
| 0.2 | Конфиг по умолчанию `resources/defaults.yaml` + pydantic-схема и загрузчик (`core/config.py`): допуски геометрии, число бэкапов, шрифт — без магических чисел в коде | 1 | ✅ |
| 0.3 | Геометрические примитивы (`core/geometry/primitives.py`): точка, отрезок, дуга (G2/G3-форма), окружность, контур; длина, точный bbox дуги, замкнутость с допуском, JSON-сериализация; тесты на квадранты и вырожденные дуги | 3 | ✅ |
| 0.4 | Pydantic-модели домена (`core/models.py`): все 17 сущностей раздела 6, перечисления с русскими подписями, структурные инварианты (сквозные операции без глубины, паз без ширины — ошибка, экспорт УП без проверок — ошибка и т.д.) | 4 | ✅ |
| 0.5 | ORM SQLAlchemy 2.x (`core/storage/orm.py`), 17 таблиц, JSON-колонки для контуров и параметров, агрегаты с delete-orphan | 3 | ✅ |
| 0.6 | Alembic: программный запуск без консоли (`core/storage/migrate.py`), начальная миграция `0001`, тест «автогенерация не находит расхождений» | 2 | ✅ |
| 0.7 | Подключение к SQLite (`core/storage/db.py`): WAL, внешние ключи, резервная копия через backup API при каждом запуске с ротацией N последних | 2 | ✅ |
| 0.8 | Репозиторий (`core/storage/repository.py`): CRUD pydantic ↔ ORM, рекурсивные агрегаты (проект → детали → операции; задание → экземпляры, раскрои; профиль → правила) | 3 | ✅ |
| 0.9 | Папки данных (`core/paths.py`): `CAM_DATA_DIR` → `%LOCALAPPDATA%` в сборке → `data/` в разработке | 0.5 | ✅ |
| 0.10 | Пустое главное окно PySide6: 7 вкладок раздела 17 (заглушки с номером этапа), меню, крупный шрифт из конфига; smoke-тест в offscreen | 2 | ✅ |
| 0.11 | CLI без Qt (`cam-cli`): `version`, `db status/upgrade/backup` | 1 | ✅ |
| 0.12 | Тест-страж «`cam.core` не импортирует Qt» | 0.5 | ✅ |
| 0.13 | Документы: `SPEC.md`, `PLAN.md`, `DECISIONS.md`, `CHANGELOG.md`, `QUESTIONS.md`, `README.md` | 1 | ✅ |
| | **Итого M0** | **25** | |

## Этап M1 — импорт DXF (ждёт ответов на вопросы 16–19 из `QUESTIONS.md`)

Черновая разбивка, будет уточнена после получения реальных DXF-файлов:

| # | Задача | Оценка |
|---|--------|-------:|
| 1.1 | `dxf/reader.py`: ezdxf → примитивы; блоки рекурсивно, OCS/extrusion → WCS, фактический цвет (BYLAYER/BYBLOCK, ACI и RGB), ATTRIB, `$INSUNITS` + проверка правдоподобия габарита | 8 |
| 1.2 | `geometry/flatten.py`: сплайны/эллипсы → полилинии с хордовым допуском; дуги остаются дугами | 3 |
| 1.3 | `geometry/stitch.py`: сшивка через хэш-сетку/STRtree, удаление дубликатов и нулевых сегментов; тесты на разрывы 0.001 мм и 50 000 сегментов | 6 |
| 1.4 | `geometry/analyze.py`: площадь со знаком (с дугами), ориентация CCW/CW и нормализация, вложенность, самопересечения, «это окружность?» | 6 |
| 1.5 | `dxf/splitter.py`: кластеризация по вложенности, режим «один файл = одна деталь», имя детали по приоритету источников | 4 |
| 1.6 | Фикстуры: реальные DXF заказчика + синтетические (отрицательный extrusion, три уровня вложенности, дубликаты) | 3 |
| 1.7 | Канва `ui/canvas`: QGraphicsView, один item на деталь/операцию, зум к курсору, панорама пробелом, LOD | 8 |
| 1.8 | Панель свойств вектора и ручное назначение операции; сохранение деталей в проект | 5 |
| | **Итого M1 (ориентир)** | **43** |
